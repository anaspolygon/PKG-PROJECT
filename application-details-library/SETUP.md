# Setup Instructions

**Install dependencies**:

```bash
npm install
```

**Build the library**:

```bash
npm run build
```

**Test locally** (optional):

```bash
npm link
# In your test project:
npm link @polygontech/application-details
```

**Publish to NPM**:

```bash
npm login
npm publish
```
